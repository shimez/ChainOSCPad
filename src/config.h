#pragma once

// Arduino IDE does not automatically add the PlatformIO include/ directory to
// its header search path. This wrapper keeps one shared configuration source.
#include "../include/config.h"

